# Contributing

## Setup
1. Clone the repo.
2. Run setup script (if available) or install dependencies (`npm install` / `pip install -r requirements.txt`).

## Development
- Create a branch for your feature.
- Follow `.editorconfig` settings.
- Run tests locally before pushing.

## Pull Requests
- Use the provided PR template.
- Ensure all CI checks pass.
